<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- electron-builder defaults for a hardened-runtime Electron app. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Required for microphone capture AND the Core Audio tap that powers
         system-audio loopback (Electron 39+); without it the tap dies
         silently and getDisplayMedia returns a dead audio track. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
